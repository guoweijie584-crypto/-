---
phase: SHPL-02-three-stage-combat-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - src/app/appState.js
  - src/content/demoContent.js
  - src/game/GameApp.js
  - src/game/gameState.js
  - src/game/weapons.js
  - src/ui/shell.js
  - src/styles.css
  - tests/app-shell.test.js
  - tests/game-contract.test.js
  - tests/weapon-contract.test.js
autonomous: true
requirements:
  - GAME-01
  - GAME-02
user_setup: []
must_haves:
  truths:
    - GAME-01 user can choose sword, blade, or spear before entering the city.
    - GAME-02 sword, blade, and spear have distinct combat behavior, not stat-only labels.
    - D-01 strong mechanical weapon differentiation is implemented.
    - D-02 sword has sword-wave identity, blade has broad/circular slash identity, and spear has thrust/dash penetration identity.
    - D-03 each weapon has a weapon-specific 3-upgrade pool.
    - D-04 existing movement, attack, throw, ultimate, HUD, particles, damage text, and screen shake paths remain preserved while weapon behavior changes.
    - UI-SPEC weapon labels and traits are visible: `剑`/`灵活剑气`, `刀`/`横扫重击`, `枪`/`突进穿刺`.
  artifacts:
    - `src/game/weapons.js` exports weapon definitions and upgrade pools.
    - `src/game/GameApp.js` consumes selected weapon through `mountGame()` options or a public method.
    - `src/ui/shell.js` renders selectable weapon buttons and a Phase 2 upgrade surface.
    - `tests/weapon-contract.test.js` exists.
  key_links:
    - `demoContent.weapons` -> `renderShell()` weapon buttons -> app state selected weapon -> `mountGame()` selected weapon.
    - selected weapon -> `performAttack()` behavior -> weapon-specific upgrade pool.
---

<objective>
Implement weapon selection and distinct sword/blade/spear combat behavior.

Purpose: Make the pre-run weapon choice materially affect gameplay and upgrade choices.
Output: Data-driven weapon definitions, selected-weapon propagation, distinct attack behavior, selectable weapon-specific upgrades, shell tests, and game contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-PATTERNS.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-03-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Integrity | Weapon selection bridge | mitigated | Validate selected weapon IDs against local `WEAPONS` definitions; unknown IDs fall back to `sword`. |
| T-02 | XSS | Upgrade and weapon DOM | mitigated | Render fixed local weapon/upgrade strings via trusted constants or `textContent`; do not render user input. |
| T-03 | Availability | Upgrade state | mitigated | Upgrade selection must pause combat and resume after one selected upgrade; tests verify `stop()` still works. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add weapon data contracts</name>
  <files>src/game/weapons.js, src/content/demoContent.js, tests/weapon-contract.test.js</files>
  <read_first>src/content/demoContent.js, src/game/gameState.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md</read_first>
  <action>Create `src/game/weapons.js` exporting `WEAPON_IDS`, `WEAPONS`, `WEAPON_UPGRADES`, `getWeaponDefinition(weaponId)`, and `getWeaponUpgrades(weaponId)`. Define exactly three weapon IDs: `sword`, `blade`, `spear`. Each weapon must include `id`, `name`, `trait`, `attack`, and `visual` fields. Encode sword as lower cooldown/moderate range/sword wave, blade as slower cooldown/wider arc/heavier damage/knockback, and spear as narrow reach plus dash/thrust penetration. Define exactly three upgrades per weapon with `id`, `title`, `desc`, and `apply(player)` fields. Update `src/content/demoContent.js` weapon traits to the UI-SPEC copy: `灵活剑气`, `横扫重击`, `突进穿刺`. Add `tests/weapon-contract.test.js` asserting three weapon IDs, three upgrades per weapon, and distinct `attack.style` values for sword/blade/spear.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/game/weapons.js` contains `export const WEAPONS`.
    - `src/game/weapons.js` contains `export const WEAPON_UPGRADES`.
    - `src/game/weapons.js` contains `sword`.
    - `src/game/weapons.js` contains `blade`.
    - `src/game/weapons.js` contains `spear`.
    - `src/content/demoContent.js` contains `灵活剑气`.
    - `src/content/demoContent.js` contains `横扫重击`.
    - `src/content/demoContent.js` contains `突进穿刺`.
    - `tests/weapon-contract.test.js` asserts each weapon has 3 upgrades.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Weapon definitions and upgrade pools exist as test-covered local data.</done>
</task>

<task type="auto">
  <name>Task 2: Wire selected weapon into the game boundary</name>
  <files>src/main.js, src/app/appState.js, src/game/GameApp.js, src/game/gameState.js, tests/game-contract.test.js</files>
  <read_first>src/main.js, src/app/appState.js, src/game/GameApp.js, src/game/gameState.js, src/game/weapons.js, tests/game-contract.test.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-PATTERNS.md</read_first>
  <action>Update app bootstrap so the selected weapon reaches the game before `start()`. Use either `mountGame(gameRoot, { selectedWeapon, emit })` plus a public `setWeapon(weaponId)` method, or a single equivalent public boundary that preserves `mountGame(container, options)`. Extend `createInitialGameState(initial = {})` so state includes `selectedWeapon`, `weapon`, and `runStats.selectedWeapon`. Ensure `getSnapshot()` includes `selectedWeapon` and `weapon` fields. Reset should return to the current app-selected weapon instead of losing it. Keep `game:ready`, `game:started`, `game:reset`, and `game:snapshot` events.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/GameApp.js` contains `selectedWeapon`.
    - `src/game/GameApp.js` returns or exposes `setWeapon`.
    - `src/game/gameState.js` contains `selectedWeapon`.
    - `src/main.js` passes selected weapon state into the game boundary.
    - `tests/game-contract.test.js` asserts `getSnapshot().selectedWeapon`.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Weapon choice flows from DOM shell into the game without breaking the public game API.</done>
</task>

<task type="auto">
  <name>Task 3: Implement weapon-specific attacks and selectable upgrades</name>
  <files>src/game/GameApp.js, src/game/gameState.js, src/game/weapons.js, src/ui/shell.js, src/styles.css, tests/app-shell.test.js, tests/game-contract.test.js</files>
  <read_first>src/game/GameApp.js, src/game/gameState.js, src/game/weapons.js, src/ui/shell.js, src/styles.css, tests/app-shell.test.js, tests/game-contract.test.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md</read_first>
  <action>Update `performAttack()` to branch by selected weapon. Sword uses fast cooldown and sword-wave behavior; blade uses wider arc/heavier damage/knockback and circular or broad sweep visuals; spear uses narrower cone/longer reach plus a short forward thrust or dash that can pierce multiple enemies. Replace the current auto-random `showUpgradeScreen()` behavior with deliberate player choice: emit `game:upgrade-available` with three upgrades from `WEAPON_UPGRADES[selectedWeapon]`, render a DOM upgrade panel with heading `选择功法`, and apply exactly the clicked upgrade before resuming `playing`. Track selected upgrade IDs/titles in `runStats.selectedUpgrades`. Add tests for upgrade panel copy and snapshot upgrade state; do not implement full skill trees.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/GameApp.js` contains `performAttack`.
    - `src/game/GameApp.js` contains `sword`.
    - `src/game/GameApp.js` contains `blade`.
    - `src/game/GameApp.js` contains `spear`.
    - `src/game/GameApp.js` contains `game:upgrade-available`.
    - `src/ui/shell.js` contains `选择功法`.
    - `src/app` or `src/ui` files contain `game:upgrade-selected` or equivalent upgrade selection event.
    - `tests/app-shell.test.js` asserts `选择功法`.
    - `tests/game-contract.test.js` asserts snapshot includes `selectedUpgrades` or `runStats.selectedUpgrades`.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Three weapon choices feel mechanically distinct and level-up choices come from weapon-specific pools.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `package.json` does not contain `phaser`.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
- [ ] Browser check confirms sword, blade, and spear selection produce visibly different attack behavior.
</verification>

<success_criteria>

- GAME-01 and GAME-02 are covered.
- Weapon selection is no longer cosmetic.
- Each weapon has a 3-upgrade pool and deliberate upgrade selection.
- `mountGame()` remains the public integration point.
- Phase 3 content remains out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-02-three-stage-combat-loop/02-01-SUMMARY.md`
</output>

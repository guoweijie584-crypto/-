---
phase: SHPL-02-three-stage-combat-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/game/GameApp.js
  - src/game/gameState.js
  - src/game/boss.js
  - src/game/enemies.js
  - src/game/rendering.js
  - src/game/runSummary.js
  - src/ui/shell.js
  - src/styles.css
  - tests/run-summary.test.js
  - tests/game-contract.test.js
  - tests/app-shell.test.js
autonomous: false
requirements:
  - GAME-04
  - GAME-05
user_setup: []
must_haves:
  truths:
    - GAME-04 user fights the final `雾甲守将` boss.
    - GAME-05 user can complete the final boss encounter and reach a clear win state within a 3-5 minute target experience.
    - D-12 boss has two phases and gains fog armor or summons paper enemies at half health.
    - D-13 run summary includes selected weapon, kills, remaining HP, completion time, victory flag, stage completion, echo fragments, boss phase, selected upgrades, and damage-taken count.
    - D-14 Phase 2 shows a victory panel plus `江湖游历卡` placeholder shell.
    - D-15 Phase 2 does not generate AI title/comment, finalized culture-card body, or route recommendation.
  artifacts:
    - `src/game/boss.js` defines boss data/behavior or equivalent exports.
    - `src/game/runSummary.js` defines summary creation.
    - `src/ui/shell.js` renders `#victory-panel` or `[data-panel="victory"]`.
    - `tests/run-summary.test.js` exists.
  key_links:
    - city tower completion -> boss start -> boss defeat -> `game:completed` -> run summary -> victory panel.
---

<objective>
Implement the final boss, victory state, run summary, and Phase 2 victory UI.

Purpose: Complete the 3-5 minute gameplay loop and produce the data Phase 3 will use for culture cards, AI fallback copy, travel card, and route recommendation.
Output: Two-phase `雾甲守将`, explicit victory state, standard run summary, victory panel with `江湖游历卡` placeholder, tests, and browser verification checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-PATTERNS.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-01-SUMMARY.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-02-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07 | Availability | Boss loop | mitigated | Boss timers, telegraphs, summons, and projectiles must have bounded durations and clear cleanup. |
| T-08 | Integrity | Run summary | mitigated | Summary is generated from local numeric state and fixed stage/weapon IDs; tests assert required fields. |
| T-09 | Scope creep | Victory UI | mitigated | Victory panel may show performance data and placeholder copy only; no AI/culture/route generation in Phase 2. |
| T-10 | XSS | Victory panel | mitigated | Victory text is derived from trusted local labels and numeric stats; render via controlled DOM update. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement two-phase boss encounter</name>
  <files>src/game/boss.js, src/game/GameApp.js, src/game/gameState.js, src/game/enemies.js, src/game/rendering.js, tests/game-contract.test.js</files>
  <read_first>src/game/GameApp.js, src/game/gameState.js, src/game/enemies.js, src/game/rendering.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md</read_first>
  <action>Create `src/game/boss.js` exporting boss helpers such as `createBoss`, `updateBoss`, and `isBossEnemy` or equivalent. Add `mist-armor-general` / `雾甲守将` as the boss identity. Start boss mode after the city tower objective completes. Boss Phase 1 uses charge and area slash telegraphs; Phase 2 begins at half health and adds fog armor shield and/or `paper-doll` summons. Add `game:boss-started` and boss status in snapshots with `active`, `phase`, `hp`, `maxHp`, and `title`. Rendering must show boss HP/status, charge/slash warning, and fog armor signal using UI-SPEC colors.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/boss.js` contains `雾甲守将` or `mist-armor-general`.
    - `src/game/boss.js` exports `createBoss` or equivalent boss creation helper.
    - `src/game/GameApp.js` contains `game:boss-started`.
    - `src/game/GameApp.js` contains `boss`.
    - `src/game/rendering.js` contains `drawBoss` or equivalent boss rendering support.
    - `tests/game-contract.test.js` asserts snapshot includes `boss`.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Final boss starts after stage objectives and exposes testable boss state.</done>
</task>

<task type="auto">
  <name>Task 2: Add victory state and standard run summary</name>
  <files>src/game/runSummary.js, src/game/GameApp.js, src/game/gameState.js, tests/run-summary.test.js, tests/game-contract.test.js</files>
  <read_first>src/game/GameApp.js, src/game/gameState.js, src/game/stages.js, src/game/weapons.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md</read_first>
  <action>Create `src/game/runSummary.js` exporting `createRunSummary(state)` and any small helpers needed. When boss HP reaches zero, set `gameState` to `victory`, stop combat simulation, create `runSummary`, and emit `game:completed` with the summary. Summary must include `selectedWeapon`, `kills`, `remainingHp`, `completionTime`, `victory`, `stages`, `echoFragments`, `bossPhaseReached`, `selectedUpgrades`, and `damageTaken`. Track `runStats.damageTaken`, `runStats.selectedUpgrades`, and per-stage completion status during gameplay. Add `tests/run-summary.test.js` asserting the exact summary keys and expected values for a constructed state.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/runSummary.js` contains `export function createRunSummary`.
    - `src/game/runSummary.js` contains `selectedWeapon`.
    - `src/game/runSummary.js` contains `remainingHp`.
    - `src/game/runSummary.js` contains `completionTime`.
    - `src/game/runSummary.js` contains `echoFragments`.
    - `src/game/runSummary.js` contains `damageTaken`.
    - `src/game/GameApp.js` contains `game:completed`.
    - `src/game/GameApp.js` contains `victory`.
    - `tests/run-summary.test.js` asserts all required summary keys.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Boss defeat produces an explicit victory state and standard Phase 2 run summary.</done>
</task>

<task type="auto">
  <name>Task 3: Render victory panel and final automated verification</name>
  <files>src/ui/shell.js, src/styles.css, tests/app-shell.test.js, tests/game-contract.test.js, tests/run-summary.test.js</files>
  <read_first>src/ui/shell.js, src/styles.css, tests/app-shell.test.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md</read_first>
  <action>Update the DOM shell to render a hidden victory surface with `#victory-panel` or `[data-panel="victory"]`. On `game:completed`, show `破阵成功`, selected weapon, kills, completion time, remaining HP, stage completion, echo fragments, damage taken, boss phase, and the placeholder copy `江湖游历卡待生成` plus `通关数据已记录，游历卡将在下一阶段生成。`. Do not show generated AI title/comment, finalized culture-card body, or route recommendation. Update app-state bridge if needed so shell receives completion payload. Run the full automated verification set.</action>
  <verify>npm test && npm run build && git diff --name-only -- gemini-code-1779459048019_副本.html</verify>
  <acceptance_criteria>
    - `src/ui/shell.js` contains `victory-panel` or `data-panel="victory"`.
    - `src/ui/shell.js` contains `破阵成功`.
    - `src/ui/shell.js` contains `江湖游历卡待生成`.
    - `src/ui/shell.js` contains `通关数据已记录，游历卡将在下一阶段生成。`.
    - `src/ui/shell.js` does not contain `夜灯破雾侠`.
    - `src/ui/shell.js` does not contain `老街 -> 古井 -> 城楼`.
    - `tests/app-shell.test.js` asserts `破阵成功`.
    - `npm test` exits 0.
    - `npm run build` exits 0.
    - `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
  </acceptance_criteria>
  <done>Victory UI is Phase-2 scoped and automated verification passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2 complete gameplay loop: weapon selection, three stage objectives, stage-specific enemies, two-phase boss, victory state, run summary, and Phase 2 victory panel.</what-built>
  <how-to-verify>Open the local dev URL. Select each weapon across runs to confirm distinct attack feel. Complete 老街灯影阵, 古井回声阵, 城楼镇妖阵, defeat 雾甲守将, and confirm the victory panel shows `破阵成功`, performance stats, and `江湖游历卡待生成` placeholder without AI/culture/route final content.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
- [ ] Browser visual/gameplay check confirms full Phase 2 loop.
- [ ] Human visual verification checkpoint is approved.
</verification>

<success_criteria>

- GAME-04 is fully covered by stage enemies and final boss.
- GAME-05 is covered by boss defeat, victory state, and summary output.
- Victory summary includes all D-13 fields.
- Victory panel is useful for Phase 2 demo but does not enter Phase 3 AI/culture/route scope.
- Phase 2 is ready for verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-02-three-stage-combat-loop/02-03-SUMMARY.md`
</output>

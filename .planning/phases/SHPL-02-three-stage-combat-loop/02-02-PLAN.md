---
phase: SHPL-02-three-stage-combat-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/game/GameApp.js
  - src/game/gameState.js
  - src/game/stages.js
  - src/game/enemies.js
  - src/game/rendering.js
  - src/ui/shell.js
  - src/styles.css
  - tests/stage-contract.test.js
  - tests/game-contract.test.js
  - tests/app-shell.test.js
autonomous: true
requirements:
  - GAME-03
  - GAME-04
user_setup: []
must_haves:
  truths:
    - GAME-03 user progresses through `老街灯影阵`, `古井回声阵`, and `城楼镇妖阵` in order.
    - GAME-04 user fights stage-appropriate enemies including `灯影妖`, `纸人怪`, and `井影`.
    - D-05 each stage uses a different objective type.
    - D-06 old street requires fighting and lighting 3 lamps.
    - D-07 ancient well requires collecting echo fragments and dodging echo waves.
    - D-08 city tower requires defending the gate or breaking talismans before boss.
    - D-09 stage flow is sequential: 老街灯影阵 -> 古井回声阵 -> 城楼镇妖阵 -> 雾甲守将 boss -> victory.
    - D-10 enemy names and behavior move into target domain: 灯影妖, 纸人怪, 井影, 雾甲守将.
    - D-11 stage enemies have distinct behavior: 灯影妖 fast chase, 纸人怪 group surround, 井影 ranged/ripple interference.
    - UI-SPEC stage/objective selectors exist for `#stage-panel` or `[data-panel="stage"]` and `[data-hud="objective"]`.
  artifacts:
    - `src/game/stages.js` defines ordered stage data.
    - `src/game/enemies.js` defines domain enemy data and behavior helpers.
    - `GameApp` snapshots include current stage and objective status.
    - `tests/stage-contract.test.js` exists.
  key_links:
    - stage definitions -> spawn profile -> enemy definitions -> objective progress -> snapshot/UI.
---

<objective>
Implement the three-stage scenic combat loop and stage-specific enemy mapping.

Purpose: Turn endless survival into a sequential objective-based route through 老街, 古井, and 城楼.
Output: Ordered stage data, objective state, stage transitions, stage-specific enemy spawns, Canvas objective visuals, DOM objective HUD, and contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-PATTERNS.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-01-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04 | Integrity | Stage progression | mitigated | Stage progression is data-driven and sequential; completion checks advance only to the next known stage ID. |
| T-05 | Availability | Objective hazards | mitigated | Echo-wave and objective updates run inside the existing loop and must not allocate unbounded arrays; stale hazards are filtered by duration. |
| T-06 | XSS | Stage HUD | mitigated | Stage titles/objectives come from trusted local `STAGES` constants and are rendered through controlled DOM updates. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add stage definitions and objective state</name>
  <files>src/game/stages.js, src/game/gameState.js, src/game/GameApp.js, tests/stage-contract.test.js, tests/game-contract.test.js</files>
  <read_first>src/game/gameState.js, src/game/GameApp.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md</read_first>
  <action>Create `src/game/stages.js` exporting `STAGE_IDS`, `STAGES`, `getStageDefinition(stageId)`, and `createStageProgress(stageId)`. Define exactly three ordered stages with IDs `old-street`, `ancient-well`, `city-tower`, titles `老街灯影阵`, `古井回声阵`, `城楼镇妖阵`, and objective copy from UI-SPEC. Extend game state with `currentStageIndex`, `currentStageId`, `stageStatus`, and `objectives`. Snapshot must include `stage` with `id`, `title`, `index`, `total`, and `objective`. Add `tests/stage-contract.test.js` asserting order, titles, objective types, and target values: lamps target 3, fragments target 5 or a similarly low configured number, and city-tower objective exists.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/game/stages.js` contains `export const STAGES`.
    - `src/game/stages.js` contains `old-street`.
    - `src/game/stages.js` contains `ancient-well`.
    - `src/game/stages.js` contains `city-tower`.
    - `src/game/stages.js` contains `老街灯影阵`.
    - `src/game/stages.js` contains `古井回声阵`.
    - `src/game/stages.js` contains `城楼镇妖阵`.
    - `src/game/gameState.js` contains `currentStageIndex`.
    - `tests/stage-contract.test.js` asserts all three stage IDs in order.
    - `tests/game-contract.test.js` asserts snapshot includes `stage`.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Ordered stage data and objective state are present and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement old street and ancient well objectives</name>
  <files>src/game/GameApp.js, src/game/gameState.js, src/game/stages.js, src/game/rendering.js, src/ui/shell.js, src/styles.css, tests/game-contract.test.js, tests/app-shell.test.js</files>
  <read_first>src/game/GameApp.js, src/game/gameState.js, src/game/stages.js, src/game/rendering.js, src/ui/shell.js, src/styles.css, .planning/phases/SHPL-02-three-stage-combat-loop/02-UI-SPEC.md</read_first>
  <action>Implement the Stage 1 and Stage 2 objective loops. For `old-street`, create 3 lamp objective objects in game state, render them as warm gold markers, let the player light a lamp when close enough after enough stage combat pressure, and mark objective progress as `litLamps / 3`. For `ancient-well`, create echo fragments, render them as teal collectibles, increment `echoFragments` on pickup, and spawn expanding echo-wave hazards that can damage the player or count as avoided hazards. Add `advanceStage()` so completing old street moves to ancient well and completing ancient well moves to city tower. Emit `game:stage-changed` and `game:objective-updated`; continue `game:snapshot`. Add or update shell stage HUD with `#stage-panel` or `[data-panel="stage"]` and `[data-hud="objective"]`.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/GameApp.js` contains `advanceStage`.
    - `src/game/GameApp.js` contains `game:stage-changed`.
    - `src/game/GameApp.js` contains `game:objective-updated`.
    - `src/game/rendering.js` contains `drawLamp` or equivalent lamp rendering function.
    - `src/game/rendering.js` contains `drawEcho` or equivalent echo rendering function.
    - `src/ui/shell.js` contains `data-panel="stage"` or `stage-panel`.
    - `src/ui/shell.js` contains `data-hud="objective"`.
    - `tests/app-shell.test.js` asserts `老街灯影阵` or stage panel root.
    - `tests/game-contract.test.js` asserts snapshot includes objective progress.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Old street and ancient well are playable objective stages with visible progress.</done>
</task>

<task type="auto">
  <name>Task 3: Add stage-specific enemies and city tower pre-boss objective</name>
  <files>src/game/enemies.js, src/game/GameApp.js, src/game/gameState.js, src/game/stages.js, src/game/rendering.js, tests/stage-contract.test.js, tests/game-contract.test.js</files>
  <read_first>src/game/GameApp.js, src/game/gameState.js, src/game/stages.js, src/game/rendering.js, .planning/phases/SHPL-02-three-stage-combat-loop/02-CONTEXT.md, .planning/phases/SHPL-02-three-stage-combat-loop/02-RESEARCH.md</read_first>
  <action>Create `src/game/enemies.js` exporting `ENEMY_TYPES`, `ENEMIES`, `createEnemy(type, position, gameTime)`, and behavior helpers as needed. Replace prototype enemy IDs with domain IDs: `lamp-shadow`, `paper-doll`, `well-shadow`. Stage 1 spawn profile uses `lamp-shadow` and `paper-doll`; Stage 2 uses `well-shadow`; Stage 3 uses `paper-doll` plus tougher pre-boss pressure. Implement `lamp-shadow` as fast chase, `paper-doll` as group surround/chase, and `well-shadow` as ranged or ripple interference. Add city tower pre-boss objective using either gate defense or talisman breaking; use `talisman` markers if choosing break objective. Completion should set a pre-boss status but not implement the boss itself in this plan.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/enemies.js` contains `lamp-shadow`.
    - `src/game/enemies.js` contains `paper-doll`.
    - `src/game/enemies.js` contains `well-shadow`.
    - `src/game/enemies.js` exports `createEnemy`.
    - `src/game/GameApp.js` no longer uses `neutral_animal`, `silly_dog`, or `fierce_monster` as active spawn IDs.
    - `src/game/stages.js` maps stage enemy sets to domain enemy IDs.
    - `src/game/rendering.js` contains rendering support for the city tower objective marker.
    - `tests/stage-contract.test.js` asserts stage enemy sets use domain IDs.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Stage-specific enemies and city tower pre-boss objective are integrated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
- [ ] Browser check confirms stage titles progress in order and objective HUD updates.
</verification>

<success_criteria>

- GAME-03 is covered by ordered stage progression.
- GAME-04 is partially covered by stage enemies before the final boss.
- Each stage has a different visible objective.
- Stage status is exposed through game snapshots and DOM HUD.
- Phase 3 culture-card content is not implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-02-three-stage-combat-loop/02-02-SUMMARY.md`
</output>

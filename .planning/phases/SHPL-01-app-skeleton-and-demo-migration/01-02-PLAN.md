---
phase: SHPL-01-app-skeleton-and-demo-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/game/GameApp.js
  - src/game/gameState.js
  - src/game/rendering.js
  - src/game/effects.js
  - src/game/input.js
  - src/styles.css
  - tests/game-contract.test.js
autonomous: true
requirements:
  - APP-02
user_setup: []
must_haves:
  truths:
    - APP-02 user can play the migrated Canvas combat loop without losing movement, attack, throw, ultimate, enemy, upgrade, HUD, and feedback behavior.
    - D-02 Canvas 2D remains the game renderer and Phaser 3 is not introduced in Phase 1.
    - D-05 the reference demo HTML remains unchanged.
    - D-06 migrated game preserves existing behavior paths for movement, attack, throw, ultimate, enemy spawning, EXP/upgrade, HUD, particles, damage text, and screen shake.
    - D-07 mountGame exposes a stable API for later phases.
  artifacts:
    - src/game/GameApp.js contains the migrated game loop.
    - src/game/gameState.js contains player/world state creation or equivalent exports.
    - src/game/rendering.js contains Canvas rendering helpers or equivalent exports.
    - src/game/effects.js contains particle/damage text helpers or equivalent exports.
    - src/game/input.js contains keyboard/mouse input setup or equivalent exports.
    - tests/game-contract.test.js exists.
  key_links:
    - mountGame() creates Canvas, installs input, starts update/render loop, and updates DOM/HUD state.
    - migrated game code preserves function equivalents from the old HTML reference.
---

<objective>
Migrate the existing Canvas combat demo into the Vite app.

Purpose: Preserve the working action prototype inside the new app shell without changing gameplay scope.
Output: Modular game code that keeps movement, combat, enemies, upgrades, HUD feedback, and visual effects working through `mountGame()`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-RESEARCH.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-PATTERNS.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-01-SUMMARY.md
@gemini-code-1779459048019_副本.html
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04 | Integrity | Reference demo | mitigated | Treat `gemini-code-1779459048019_副本.html` as read-only and compare migrated behavior against it. |
| T-05 | Availability | Game loop | mitigated | `mountGame().stop()` must cancel animation/input lifecycle enough for tests and future remounts. |
| T-06 | XSS | HUD text | mitigated | HUD text is derived from numeric game state and fixed strings; do not insert unsanitized user input. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract game state and input from the reference demo</name>
  <files>src/game/GameApp.js, src/game/gameState.js, src/game/input.js</files>
  <read_first>gemini-code-1779459048019_副本.html, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md, src/game/GameApp.js</read_first>
  <action>Read the old HTML script and migrate the player/world state into `src/game/gameState.js`, preserving fields for `gameState`, `kills`, `gameTime`, `spawnTimer`, `screenShake`, `camera`, `terrain`, `player`, `enemies`, `droppedWeapons`, `projectiles`, `particles`, and `damageTexts`. Create input setup in `src/game/input.js` that supports the existing keyboard and mouse behavior: WASD/arrow movement, Space ultimate, left-click attack hold, right-click throw, mouse aim, and context-menu prevention. Wire these into `mountGame()` without changing `gemini-code-1779459048019_副本.html`.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/game/gameState.js` contains `createInitialGameState`.
    - `src/game/gameState.js` contains `player`.
    - `src/game/gameState.js` contains `enemies`.
    - `src/game/input.js` contains `keydown`.
    - `src/game/input.js` contains `contextmenu`.
    - `src/game/input.js` contains `isLeftDown`.
    - `git diff --name-only` does not include `gemini-code-1779459048019_副本.html`.
  </acceptance_criteria>
  <done>State and input setup are migrated into modules while preserving the reference file.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate combat, progression, and effects behavior</name>
  <files>src/game/GameApp.js, src/game/gameState.js, src/game/effects.js</files>
  <read_first>gemini-code-1779459048019_副本.html, src/game/GameApp.js, src/game/gameState.js, src/game/input.js</read_first>
  <action>Migrate behavior equivalents for `distance`, `spawnText`, `spawnParticles`, `triggerUltimate`, `throwWeapon`, `spawnEnemy`, `performAttack`, `damageEnemy`, `checkLevelUp`, `showUpgradeScreen`, `updateUI`, `hurtPlayer`, and `update`. Keep the old behavior values visible in the migrated code: `MAP_SIZE = 4000`, initial `player.hp = 100`, `player.speed = 4.5`, `attackCooldown = 20`, ultimate radius `250`, thrown weapon speed `16`, and upgrade titles from the reference demo. If helper modules are used, export/import them through `GameApp.js` so `mountGame()` remains the public integration point.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/GameApp.js` or imported game modules contain `triggerUltimate`.
    - `src/game/GameApp.js` or imported game modules contain `throwWeapon`.
    - `src/game/GameApp.js` or imported game modules contain `performAttack`.
    - `src/game/GameApp.js` or imported game modules contain `damageEnemy`.
    - `src/game/GameApp.js` or imported game modules contain `checkLevelUp`.
    - migrated game code contains `MAP_SIZE = 4000` or `MAP_SIZE=4000`.
    - migrated game code contains `attackCooldown: 20`.
    - migrated game code contains `瞬狱影杀阵`.
    - `package.json` does not contain `phaser`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Core combat/progression behavior is available in the Vite game module.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Canvas rendering and verify game contract</name>
  <files>src/game/GameApp.js, src/game/rendering.js, src/game/effects.js, src/styles.css, tests/game-contract.test.js</files>
  <read_first>gemini-code-1779459048019_副本.html, src/game/GameApp.js, src/styles.css, tests/app-shell.test.js</read_first>
  <action>Migrate Canvas rendering equivalents for terrain, player, swing arc, enemies, enemy HP bars, dropped weapons, projectiles, particles, and damage text into `src/game/rendering.js` or equivalent functions called by `GameApp.js`. Ensure `render()` clears the Canvas, applies camera translation, draws a nonblank world, and supports screen shake. Add `tests/game-contract.test.js` that imports `mountGame`, mounts it into a jsdom container, verifies a Canvas with id `gameCanvas` exists, verifies `getSnapshot()` returns `status`, `player`, and `kills`, and verifies `stop()` can be called without throwing. Keep CSS Canvas full-bleed inside `#game-root`.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/game/rendering.js` contains `drawPlayer`.
    - `src/game/rendering.js` contains `drawEnemy`.
    - `src/game/rendering.js` contains `render`.
    - `tests/game-contract.test.js` contains `mountGame`.
    - `tests/game-contract.test.js` asserts `gameCanvas`.
    - `tests/game-contract.test.js` asserts `getSnapshot`.
    - `npm test` exits 0.
    - `npm run build` exits 0.
    - `git diff --name-only` does not include `gemini-code-1779459048019_副本.html`.
  </acceptance_criteria>
  <done>Migrated game renders through Canvas and has a stable smoke-tested contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only` does not include `gemini-code-1779459048019_副本.html`.
- [ ] Browser check confirms movement, attack, throw, ultimate, enemy spawning, upgrade modal, and game-over modal still work.
</verification>

<success_criteria>

- All tasks completed.
- APP-02 behavior preservation is verified by source assertions, tests, build, and manual/browser check.
- `mountGame()` remains the only public game entry point needed by the app shell.
- Old reference HTML remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-02-SUMMARY.md`
</output>

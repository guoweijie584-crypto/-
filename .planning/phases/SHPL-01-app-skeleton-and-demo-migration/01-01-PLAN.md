---
phase: SHPL-01-app-skeleton-and-demo-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - index.html
  - src/main.js
  - src/styles.css
  - src/app/appState.js
  - src/content/demoContent.js
  - src/game/GameApp.js
  - src/ui/shell.js
  - tests/app-shell.test.js
autonomous: true
requirements:
  - APP-01
  - APP-03
user_setup: []
must_haves:
  truths:
    - APP-01 user can open a Vite-served app with a visible game surface in the first screen.
    - APP-03 DOM regions exist for weapon selection, narrator dialogue, culture cards, completion card, and route output.
    - D-01 vanilla Vite with ES modules is used, not React/Vue/Svelte.
    - D-03 Lucide is used for DOM shell icon rendering.
    - D-04 Vitest plus jsdom is used for lightweight Phase 1 smoke tests.
    - D-08 DOM/CSS overlays are used around the Canvas.
  artifacts:
    - package.json contains dev, build, preview, and test scripts.
    - index.html exists and loads /src/main.js.
    - src/main.js exists and mounts the app.
    - src/game/GameApp.js exports mountGame.
    - src/ui/shell.js exports renderShell.
    - tests/app-shell.test.js exists.
  key_links:
    - index.html -> src/main.js -> renderShell() -> mountGame()
    - renderShell() creates the DOM roots consumed by mountGame()
---

<objective>
Create the Vite + Canvas walking skeleton for Phase 1.

Purpose: Establish the modern H5 app shell before migrating gameplay so execution has a working baseline.
Output: A vanilla Vite project with app entry, Canvas mount stub, DOM shell regions, demo content placeholders, CSS, and smoke tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-UI-SPEC.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-RESEARCH.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-PATTERNS.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information disclosure | Frontend config | mitigated | Do not add AI provider keys, tokens, or private endpoints to package.json, source files, tests, or HTML. |
| T-02 | XSS | DOM shell placeholders | mitigated | Render fixed Phase 1 placeholder strings with textContent or static markup from trusted local constants; do not render user input. |
| T-03 | Supply chain | npm dependencies | accepted | Limit new runtime dependencies to `lucide` and dev dependencies to `vite`, `vitest`, `jsdom`; verify package.json does not introduce unrelated packages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create vanilla Vite project skeleton</name>
  <files>package.json, index.html, src/main.js, src/styles.css</files>
  <read_first>.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md, .planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-UI-SPEC.md, .planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-RESEARCH.md</read_first>
  <action>Create `package.json` for a vanilla Vite app with `"type": "module"` and scripts exactly named `dev`, `build`, `preview`, and `test`. Use dependencies: runtime `lucide`; dev dependencies `vite`, `vitest`, `jsdom`. Create `index.html` with title `山河破阵录：古城夜巡`, a single root element with id `app`, and `<script type="module" src="/src/main.js"></script>`. Create `src/main.js` that imports `./styles.css`, `createAppState` from `./app/appState.js`, `demoContent` from `./content/demoContent.js`, `renderShell` from `./ui/shell.js`, and `mountGame` from `./game/GameApp.js`; then mounts the shell and game into `#app`.</action>
  <verify>npm install && npm run build</verify>
  <acceptance_criteria>
    - `package.json` contains `"dev": "vite --host 0.0.0.0"`.
    - `package.json` contains `"build": "vite build"`.
    - `package.json` contains `"test": "vitest run"`.
    - `package.json` contains `"lucide"`.
    - `package.json` contains `"vitest"`.
    - `package.json` contains `"jsdom"`.
    - `index.html` contains `山河破阵录：古城夜巡`.
    - `index.html` contains `<div id="app"></div>`.
    - `index.html` contains `/src/main.js`.
    - `src/main.js` imports `renderShell` and `mountGame`.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Vite skeleton builds and the app entry wires shell plus game mount.</done>
</task>

<task type="auto">
  <name>Task 2: Add app state and demo content contracts</name>
  <files>src/app/appState.js, src/content/demoContent.js</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md</read_first>
  <action>Create `src/app/appState.js` exporting `createAppState(initial = {})`. The returned state object must include `status`, `selectedWeapon`, `narratorText`, `unlockedCards`, `completion`, and `route`, plus `subscribe(listener)`, `emit(eventName, payload)`, `setState(patch)`, and `getSnapshot()`. Create `src/content/demoContent.js` exporting `demoContent` with `title`, `weapons`, `uiPlaceholders`, `cultureCards`, and `route`. Each `cultureCards` item must include `spotId`, `title`, `sourceNote`, `unlockStage`, and `routeOrder` even though Phase 1 only uses placeholders.</action>
  <verify>npm test -- --runInBand is not required; run `npm test` after Task 3 adds tests</verify>
  <acceptance_criteria>
    - `src/app/appState.js` contains `export function createAppState`.
    - `src/app/appState.js` contains `subscribe(listener)`.
    - `src/app/appState.js` contains `emit(eventName, payload)`.
    - `src/app/appState.js` contains `getSnapshot()`.
    - `src/content/demoContent.js` contains `spotId`.
    - `src/content/demoContent.js` contains `sourceNote`.
    - `src/content/demoContent.js` contains `unlockStage`.
    - `src/content/demoContent.js` contains `routeOrder`.
  </acceptance_criteria>
  <done>Shared state and demo content interfaces exist for later phases.</done>
</task>

<task type="auto">
  <name>Task 3: Add DOM shell, Canvas placeholder, and smoke tests</name>
  <files>src/ui/shell.js, src/game/GameApp.js, src/styles.css, tests/app-shell.test.js</files>
  <read_first>.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-UI-SPEC.md, .planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-PATTERNS.md</read_first>
  <action>Create `renderShell(root, state, content)` in `src/ui/shell.js`. It must create stable DOM roots with ids `game-root`, `weapon-panel`, `narrator-panel`, `culture-panel`, `completion-panel`, and `route-panel`, using Phase 1 copy from UI-SPEC. Use Lucide `createIcons` and imported icons for at least weapon, scroll/story, card, and map/route surfaces. Create `mountGame(container, options = {})` in `src/game/GameApp.js` that appends a `<canvas id="gameCanvas">`, sizes it to its parent, draws a nonblank placeholder scene, and returns an object with `start`, `stop`, `reset`, and `getSnapshot`. Add CSS from UI-SPEC colors/spacing so Canvas is full-bleed and shell panels overlay it. Add `tests/app-shell.test.js` with jsdom tests for shell roots and `mountGame` return contract.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/ui/shell.js` contains `export function renderShell`.
    - `src/ui/shell.js` contains `weapon-panel`.
    - `src/ui/shell.js` contains `narrator-panel`.
    - `src/ui/shell.js` imports from `lucide`.
    - `src/game/GameApp.js` contains `export function mountGame`.
    - `src/game/GameApp.js` contains `id = 'gameCanvas'` or `id="gameCanvas"` equivalent.
    - `src/game/GameApp.js` returns methods named `start`, `stop`, `reset`, and `getSnapshot`.
    - `tests/app-shell.test.js` asserts all six root ids exist.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>First-screen app shell exists, is test-covered, and builds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds.
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only` does not include `gemini-code-1779459048019_副本.html`.
- [ ] Browser or screenshot check confirms first viewport has nonblank Canvas and DOM shell regions.
</verification>

<success_criteria>

- All tasks completed.
- APP-01 first-screen Vite app shell exists.
- APP-03 DOM UI regions exist for all future product surfaces.
- No API keys or external service secrets are introduced.
- Existing reference HTML remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-01-SUMMARY.md`
</output>

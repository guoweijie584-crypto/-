---
phase: SHPL-01-app-skeleton-and-demo-migration
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/main.js
  - src/app/appState.js
  - src/ui/shell.js
  - src/content/demoContent.js
  - src/styles.css
  - tests/app-shell.test.js
  - tests/game-contract.test.js
autonomous: false
requirements:
  - APP-01
  - APP-02
  - APP-03
user_setup: []
must_haves:
  truths:
    - APP-01 Vite app opens with game visible in the first viewport.
    - APP-02 migrated combat loop remains connected after UI shell wiring.
    - APP-03 user can see DOM interface regions for weapon selection, narrator dialogue, culture cards, completion card, and route output.
    - D-09 Phase 1 reserves future product surfaces without implementing Phase 2/3 behavior.
    - D-10 the first screen is the actual app/game experience, not a marketing landing page.
  artifacts:
    - src/main.js wires app state, content, shell, and game together.
    - src/ui/shell.js renders the full Phase 1 product shell.
    - tests/app-shell.test.js covers the reserved product surfaces.
    - tests/game-contract.test.js still passes after shell wiring.
  key_links:
    - App state events can update shell placeholders from game lifecycle callbacks.
    - `renderShell()` output supplies `game-root` consumed by `mountGame()`.
    - demo content fields are visible through shell placeholders.
---

<objective>
Connect the migrated game to the product-facing DOM shell.

Purpose: Finish Phase 1 by making the first viewport feel like the real H5/PWA app, not just a raw migrated demo.
Output: Integrated shell with reserved product surfaces, app-state wiring, tests, and human visual verification checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-UI-SPEC.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-PATTERNS.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-01-SUMMARY.md
@.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-02-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07 | XSS | Product shell | mitigated | Render local demo content via textContent or controlled template constants; do not add freeform user input rendering. |
| T-08 | Integrity | App state bridge | mitigated | Use explicit event names and state patch fields; do not let UI mutate game internals directly. |
| T-09 | Availability | Visual verification server | mitigated | Dev server is only for local verification; stop or document running session in SUMMARY.md. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire app state between shell and game</name>
  <files>src/main.js, src/app/appState.js, src/game/GameApp.js, src/ui/shell.js</files>
  <read_first>src/main.js, src/app/appState.js, src/game/GameApp.js, src/ui/shell.js, .planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-CONTEXT.md</read_first>
  <action>Update `src/main.js` so `createAppState`, `demoContent`, `renderShell`, and `mountGame` are connected through one app bootstrap. The shell must render first, then `mountGame` must mount into `#game-root`. Add or preserve event names `game:ready`, `game:started`, `game:reset`, and `game:snapshot` in the app state bridge. The game may emit these events through an `emit` option, but UI code must not reach into mutable game internals.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/main.js` contains `createAppState`.
    - `src/main.js` contains `renderShell`.
    - `src/main.js` contains `mountGame`.
    - `src/main.js` contains `game-root`.
    - source files contain `game:ready`.
    - source files contain `game:snapshot`.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>App bootstrap connects state, shell, content, and game through stable boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Finish Phase 1 product shell surfaces</name>
  <files>src/ui/shell.js, src/content/demoContent.js, src/styles.css, tests/app-shell.test.js</files>
  <read_first>src/ui/shell.js, src/content/demoContent.js, src/styles.css, tests/app-shell.test.js, .planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-UI-SPEC.md</read_first>
  <action>Update `renderShell()` and CSS so the first viewport includes the title `山河破阵录：古城夜巡`, CTA copy `开始夜巡`, weapon panel heading `选择兵器`, narrator label `AI 说书人`, culture placeholder `文化线索待解锁`, completion placeholder `江湖游历卡待生成`, and route placeholder `游览路线待推荐`. Use UI-SPEC colors: `#101613`, `#25342C`, `#D7A84B`, `#54C6B2`, `#C9493D`. Keep Canvas full-bleed and overlay shell surfaces with stable dimensions so text cannot resize the game area. Update tests to assert the exact shell copy strings.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/ui/shell.js` contains `山河破阵录：古城夜巡`.
    - `src/ui/shell.js` contains `开始夜巡`.
    - `src/ui/shell.js` contains `选择兵器`.
    - `src/ui/shell.js` contains `AI 说书人`.
    - `src/ui/shell.js` contains `文化线索待解锁`.
    - `src/ui/shell.js` contains `江湖游历卡待生成`.
    - `src/ui/shell.js` contains `游览路线待推荐`.
    - `src/styles.css` contains `#101613`.
    - `src/styles.css` contains `#D7A84B`.
    - `tests/app-shell.test.js` asserts at least four exact Chinese shell copy strings.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Product shell clearly reserves all future Phase 2/3 surfaces and matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 1 automated verification</name>
  <files>tests/app-shell.test.js, tests/game-contract.test.js</files>
  <read_first>tests/app-shell.test.js, tests/game-contract.test.js, package.json, .planning/REQUIREMENTS.md</read_first>
  <action>Run the full automated verification set for Phase 1. If tests fail due to missing deterministic assertions, update only `tests/app-shell.test.js` or `tests/game-contract.test.js` to assert the intended Phase 1 contracts: shell roots, exact copy strings, `mountGame` methods, Canvas creation, and snapshot shape. Do not relax tests to merely check that files exist.</action>
  <verify>npm test && npm run build && git diff --name-only -- gemini-code-1779459048019_副本.html</verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `npm run build` exits 0.
    - `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
    - `tests/app-shell.test.js` contains `weapon-panel`.
    - `tests/game-contract.test.js` contains `getSnapshot`.
  </acceptance_criteria>
  <done>Automated verification proves Phase 1 shell/game contracts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 1 Vite + Canvas shell with migrated game and DOM product surfaces. Executor should start the dev server and provide the local URL, typically http://localhost:5173.</what-built>
  <how-to-verify>Open the URL. Check that the first viewport shows a nonblank Canvas/game surface, visible title `山河破阵录：古城夜巡`, weapon panel `选择兵器`, narrator label `AI 说书人`, culture/completion/route placeholders, and no obvious text overlap. Confirm keyboard/mouse combat still works at least for movement, attack, throw, and ultimate.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
- [ ] Human visual verification checkpoint is approved.
</verification>

<success_criteria>

- All tasks completed.
- APP-01, APP-02, and APP-03 are all covered by the integrated first viewport.
- User-approved visual check confirms Canvas and DOM overlays are coherent.
- Phase 1 is ready for execute-phase verification.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-01-app-skeleton-and-demo-migration/01-03-SUMMARY.md`
</output>

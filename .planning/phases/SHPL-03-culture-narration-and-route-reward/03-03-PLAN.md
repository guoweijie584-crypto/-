---
phase: SHPL-03-culture-narration-and-route-reward
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/app/appState.js
  - src/main.js
  - src/ui/shell.js
  - src/styles.css
  - tests/app-shell.test.js
  - tests/app-state.test.js
  - tests/narration.test.js
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
user_setup: []
must_haves:
  truths:
    - User receives a final `江湖游历卡` after completing the run.
    - Travel card shows selected weapon, performance summary, unlocked culture cards, generated title, and generated comment.
    - Route panel recommends `老街 -> 古井 -> 石桥 -> 园林 -> 城楼`.
    - Completion flow does not restore the old blocking victory popup.
  artifacts:
    - `#completion-panel` renders the final travel card.
    - `#route-panel` renders the final five-point route recommendation.
---

<objective>
Add final travel card and five-point route recommendation flow.

Purpose: Complete the AI+文旅 loop by turning a completed run into a readable reward and offline route.
Output: Final completion panel, route panel, app-state composition, styling, tests, and browser verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-CONTEXT.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-RESEARCH.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-UI-SPEC.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-PATTERNS.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-01-SUMMARY.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-02-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07 | Integrity | Route recommendation | mitigated | Build route output from ordered `demoContent.route`, not generated freeform order. |
| T-08 | XSS | Completion and route DOM | mitigated | Render dynamic values through `textContent` and fixed DOM nodes. |
| T-09 | Availability | Completion layout | mitigated | Keep copy concise and allow panel scrolling for long content. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Compose travel-card view model</name>
  <files>src/app/appState.js, src/main.js, tests/app-state.test.js</files>
  <read_first>src/app/appState.js, src/main.js, src/narration/localNarrator.js, src/content/demoContent.js</read_first>
  <action>Create or derive a completion travel-card object from `runSummary`, generated title/comment, selected weapon label, unlocked culture cards, and route data. Keep the raw run summary intact. Add tests asserting the card includes selected weapon, kills, remaining HP, completion time, title, comment, and unlocked card titles.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - Completion card state includes title and comment.
    - Completion card state includes selected weapon and performance summary.
    - Completion card state includes unlocked culture cards.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Travel-card data is composed from real gameplay and content state.</done>
</task>

<task type="auto">
  <name>Task 2: Render completion travel card</name>
  <files>src/ui/shell.js, src/styles.css, tests/app-shell.test.js</files>
  <read_first>src/ui/shell.js, src/styles.css, tests/app-shell.test.js, .planning/phases/SHPL-03-culture-narration-and-route-reward/03-UI-SPEC.md</read_first>
  <action>Add a shell method such as `updateCompletionCard(card)` that renders `江湖游历卡`, generated title, selected weapon, performance stats, unlocked culture cards, and completion comment inside `#completion-panel`. Keep `#victory-panel` absent. Update styles for dense but readable reward content.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `#completion-panel` shows `江湖游历卡`.
    - Tests assert generated title/comment render.
    - Tests assert old `#victory-panel` is absent.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Final reward card is visible in the existing shell.</done>
</task>

<task type="auto">
  <name>Task 3: Render five-point route recommendation</name>
  <files>src/ui/shell.js, src/styles.css, tests/app-shell.test.js, tests/narration.test.js</files>
  <read_first>src/ui/shell.js, src/styles.css, src/content/demoContent.js, src/narration/localNarrator.js</read_first>
  <action>Add a shell method such as `updateRoute(routeView)` that renders `夜巡游线`, the exact order `老街 -> 古井 -> 石桥 -> 园林 -> 城楼`, and stop-level visit hints from route/culture data in `#route-panel`. Use local narrator route copy where appropriate but preserve route order from structured data.</action>
  <verify>npm test && npm run build && git diff --name-only -- gemini-code-1779459048019_副本.html</verify>
  <acceptance_criteria>
    - `#route-panel` shows `夜巡游线`.
    - Route output contains `老街 -> 古井 -> 石桥 -> 园林 -> 城楼`.
    - Tests assert route order.
    - `npm test` exits 0.
    - `npm run build` exits 0.
    - Reference HTML remains untouched.
  </acceptance_criteria>
  <done>Final route recommendation completes the Phase 3 reward flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 3 AI+文旅 loop: five culture-card unlocks, local AI-style narrator/title/comment, final 江湖游历卡, and five-point route recommendation.</what-built>
  <how-to-verify>Open the local dev URL, complete a run, and confirm the narrator updates, culture cards unlock, the completion panel shows a generated title/comment and gameplay stats, and the route panel shows `老街 -> 古井 -> 石桥 -> 园林 -> 城楼` without the old blocking victory popup.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
- [ ] Browser check confirms final reward panels update after completion.
- [ ] Human visual verification checkpoint is approved.
</verification>

<success_criteria>
- ROUTE-01 through ROUTE-03 are covered.
- The full Phase 3 AI+文旅 reward loop is complete.
- Phase 4 can focus on mobile/PWA hardening without adding core reward content.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-03-culture-narration-and-route-reward/03-03-SUMMARY.md`
</output>


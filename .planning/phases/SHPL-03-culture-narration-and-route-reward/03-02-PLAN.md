---
phase: SHPL-03-culture-narration-and-route-reward
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/narration/localNarrator.js
  - src/app/appState.js
  - src/main.js
  - src/ui/shell.js
  - tests/narration.test.js
  - tests/app-shell.test.js
autonomous: true
requirements:
  - NARR-01
  - NARR-02
  - NARR-03
user_setup: []
must_haves:
  truths:
    - Narration/title/comment generation works without a real AI API.
    - Generated copy uses run summary, weapon, stage, and structured culture data.
    - Generated copy must not invent new cultural facts.
    - Narrator panel updates for onboarding, stage transitions, culture unlocks, and completion.
  artifacts:
    - `src/narration/localNarrator.js` exports pure local generation helpers.
    - `tests/narration.test.js` exists.
---

<objective>
Add local Jianghu narrator, title, comment, and route-copy fallback generation.

Purpose: Provide the AI-style layer for the文旅 game without depending on a provider API.
Output: Pure narration helpers, integration events, narrator panel updates, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-CONTEXT.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-RESEARCH.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-UI-SPEC.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-PATTERNS.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-01-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04 | Privacy | AI integration | mitigated | No network calls or provider API keys in frontend code. |
| T-05 | Integrity | Generated cultural copy | mitigated | Narrator functions receive structured content and may only reference fields passed in. |
| T-06 | XSS | Generated narration DOM | mitigated | Render generated strings through `textContent`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add local narrator module</name>
  <files>src/narration/localNarrator.js, tests/narration.test.js</files>
  <read_first>src/content/demoContent.js, src/game/runSummary.js, .planning/phases/SHPL-03-culture-narration-and-route-reward/03-RESEARCH.md</read_first>
  <action>Create `src/narration/localNarrator.js` with pure functions for onboarding, stage transition, culture unlock, completion narration, travel title, completion comment, and route recommendation copy. Use deterministic template selection based on selected weapon, victory, remaining HP, kills, cleared stages, and unlocked cards. Add tests asserting no exported function requires async/network behavior, title/comment include performance signals, and route copy includes the five route stops.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/narration/localNarrator.js` exists.
    - It exports title/comment and route-copy helpers.
    - `tests/narration.test.js` asserts five-stop route copy.
    - `tests/narration.test.js` asserts completion title/comment behavior.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Local AI-style generation is deterministic and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Wire narrator updates through app state</name>
  <files>src/app/appState.js, src/main.js, src/ui/shell.js, tests/app-shell.test.js</files>
  <read_first>src/app/appState.js, src/main.js, src/ui/shell.js, src/narration/localNarrator.js</read_first>
  <action>Update app orchestration so onboarding uses local narrator copy, stage changes update narrator text, culture unlocks show unlock narration, and completion shows final narration. Add a shell method such as `updateNarrator(text)`. Keep generated text local and synchronous.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/ui/shell.js` exposes `updateNarrator` or equivalent.
    - `main.js` imports local narrator helpers.
    - `tests/app-shell.test.js` asserts narrator panel can update.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Narrator panel reflects gameplay and reward events.</done>
</task>

<task type="auto">
  <name>Task 3: Store generated title and comment on completion</name>
  <files>src/app/appState.js, src/main.js, tests/narration.test.js, tests/app-state.test.js</files>
  <read_first>src/app/appState.js, src/main.js, src/game/runSummary.js, src/narration/localNarrator.js</read_first>
  <action>On `game:completed`, derive a completion object that includes `title`, `comment`, `summary`, and `unlockedCards`. Store it in app state without replacing the raw `runSummary`. Add tests for title/comment generation from a representative summary.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - App state keeps `runSummary` and derived `completion`.
    - Completion includes generated `title` and `comment`.
    - Tests cover the derived completion object.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Completion state now contains generated AI-style reward copy.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] Search confirms no provider API key or AI network call was added.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
</verification>

<success_criteria>
- NARR-01 through NARR-03 are covered.
- AI-style behavior is local fallback generation only.
- Completion title/comment are available for the final travel card.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-03-culture-narration-and-route-reward/03-02-SUMMARY.md`
</output>


---
phase: SHPL-03-culture-narration-and-route-reward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/demoContent.js
  - src/app/appState.js
  - src/main.js
  - src/ui/shell.js
  - src/styles.css
  - tests/content-contract.test.js
  - tests/app-state.test.js
  - tests/app-shell.test.js
autonomous: true
requirements:
  - CULT-01
  - CULT-02
  - CULT-03
  - CULT-04
user_setup: []
must_haves:
  truths:
    - Phase 3 uses the five-point route `old-street -> ancient-well -> stone-bridge -> garden-maze -> city-tower`.
    - User unlocks one culture card after each completed stage.
    - Culture-card facts are structured local data, not generated freeform.
    - Existing `#culture-panel` becomes a real unlock surface rather than a static teaser.
  artifacts:
    - `src/content/demoContent.js` includes five complete culture card records.
    - `src/app/appState.js` tracks unlocked cards and deduplicates by `spotId`.
    - `tests/content-contract.test.js` exists.
---

<objective>
Add five-point structured culture content and culture-card unlock UI.

Purpose: Convert stage clears into visible cultural rewards for 老街, 古井, 石桥, 园林, and 城楼.
Output: Complete demo culture data, unlock state, shell update methods, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-CONTEXT.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-RESEARCH.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-UI-SPEC.md
@.planning/phases/SHPL-03-culture-narration-and-route-reward/03-PATTERNS.md
@.planning/phases/SHPL-02-three-stage-combat-loop/02-03-SUMMARY.md
</context>

<threat_model>
## Threat Model

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Integrity | Culture facts | mitigated | Store facts in structured local content records and render only those fields. |
| T-02 | XSS | Culture card DOM | mitigated | Render dynamic card fields via `textContent` or node creation. |
| T-03 | Availability | Unlock events | mitigated | Deduplicate by `spotId` and keep unlock state bounded to five records. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade five-point culture content contract</name>
  <files>src/content/demoContent.js, tests/content-contract.test.js</files>
  <read_first>src/content/demoContent.js, src/game/stages.js, .planning/phases/SHPL-03-culture-narration-and-route-reward/03-CONTEXT.md</read_first>
  <action>Extend every `cultureCards` record with concise demo-ready fields: `subtitle`, `body`, `heritageHint`, `visitTip`, and `tags`. Ensure there are exactly five cards matching the five stage IDs and route order. Keep `spotId`, `title`, `sourceNote`, `unlockStage`, and `routeOrder`. Update route data so every stop has `spotId`, `title`, `routeOrder`, and a short `visitHint`. Add `tests/content-contract.test.js` asserting required fields, five IDs in order, and route/card alignment.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/content-contract.test.js` asserts five culture cards.
    - `tests/content-contract.test.js` asserts five route stops in order.
    - Every card has `sourceNote`, `body`, `heritageHint`, and `visitTip`.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Five-point culture and route data are structured and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Track culture unlocks in app state</name>
  <files>src/app/appState.js, src/main.js, tests/app-state.test.js</files>
  <read_first>src/app/appState.js, src/main.js, src/game/GameApp.js, src/game/runSummary.js</read_first>
  <action>Add app-state handling for a `culture:unlocked` event carrying a culture card. Maintain `unlockedCards` as a deduplicated array keyed by `spotId`. On `game:completed`, derive completed stage IDs from the run summary and ensure corresponding cards are unlocked. Reset should clear unlocked cards unless loading a saved run explicitly preserves them. Add tests that duplicate unlocks do not duplicate cards and that completion can unlock all completed-stage cards.</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `src/app/appState.js` handles `culture:unlocked`.
    - `tests/app-state.test.js` asserts deduplication by `spotId`.
    - `tests/app-state.test.js` asserts completion unlock behavior.
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>Culture unlock state exists and is deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Render culture unlock UI</name>
  <files>src/ui/shell.js, src/main.js, src/styles.css, tests/app-shell.test.js</files>
  <read_first>src/ui/shell.js, src/styles.css, tests/app-shell.test.js, .planning/phases/SHPL-03-culture-narration-and-route-reward/03-UI-SPEC.md</read_first>
  <action>Add a shell method such as `updateCultureCards(cards, latestCard)` that renders the latest unlocked card and a compact count/list of unlocked route points in `#culture-panel`. Wire app-state culture unlock events from `main.js`. Initial state should still show `文化线索待解锁`. Use safe DOM rendering for all dynamic content.</action>
  <verify>npm test && npm run build</verify>
  <acceptance_criteria>
    - `src/ui/shell.js` exposes `updateCultureCards` or equivalent.
    - `#culture-panel` can show `已解锁文化线索`.
    - `tests/app-shell.test.js` asserts culture panel update behavior.
    - `npm test` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Stage culture cards unlock into the DOM shell.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` succeeds.
- [ ] `npm run build` succeeds.
- [ ] `git diff --name-only -- gemini-code-1779459048019_副本.html` prints no path.
</verification>

<success_criteria>
- CULT-01 through CULT-04 are covered for the five-point route.
- Culture facts are structured local data.
- Culture panel no longer feels like a placeholder after unlocks.
</success_criteria>

<output>
After completion, create `.planning/phases/SHPL-03-culture-narration-and-route-reward/03-01-SUMMARY.md`
</output>

